void main() {
  double a = 5;
  double b = 10;
  // What is expression? What is literal/value? What is variable?
  double c = a + b + 5.0;
  String name = 'Teja' + 'Revati';

  // Arithmetic operators: +, -, *, /, %, -
  double d = 5 + 10 * 15;
  // Conditional operators: and, or, xor, <, >, ==, !=, <=, >=
  bool e = true && false;
  bool f = a < 10;
  bool g = (a < 10) && (b > 10);

  if(f) {
    // TODO
  } else {
    // TODO
  }
}